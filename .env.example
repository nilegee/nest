# .env.example

# Database connection for reset workflow (PostgreSQL connection string)
DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DBNAME

# Required to be set to TRUE for the database reset workflow to run
CONFIRM_DB_RESET=FALSE

# Optional: Supabase access token for enhanced CLI functionality
# SUPABASE_ACCESS_TOKEN=